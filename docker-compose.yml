version: "3.9"

services:
  web:
    container_name: web
    build:
      context: .
      dockerfile: ./apps/web/Dockerfile
    restart: always
    ports:
      - 4000:3000
    networks:
      - otog-network

networks:
  otog-network:
    name: otog-network
